# Shared secret with the backend (see backend/.env.example's AI_SERVICE_TOKEN).
# Both services must be configured with the exact same value.
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
INTERNAL_SERVICE_TOKEN=replace-with-generated-64-char-hex-token
