fastapi==0.139.0
uvicorn==0.24.0
pillow==12.3.0
numpy==1.26.4

# CPU-only builds, pulled from an extra index (not --index-url, that
# would replace PyPI entirely and break the packages listed above).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
torchvision==0.28.0+cpu